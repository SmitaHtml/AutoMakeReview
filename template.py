# Template for scrapers that is used by scrape.sh
import csv
import json
import requests
from bs4 import BeautifulSoup

s = requests.Session()

# csv_columns = ["name", "address", "city", "state", "zip"]
# with open("Results.csv", 'w') as outfile:
#     writer = csv.DictWriter(outfile, fieldnames = csv_columns)
#     writer.writeheader()
content = s.
# dict = {"name": " "+city, "address": address, "city": city, "state": state, "zip": zip}
# print dict
# writer.writerow(dict)
